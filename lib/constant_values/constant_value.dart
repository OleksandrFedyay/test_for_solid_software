import 'package:flutter/material.dart';

///Greeting text
const String greeting = 'Hello there';

/// [TextStyle] object that is used for customization of the [greeting] text
const TextStyle textStyle = TextStyle(
  fontWeight: FontWeight.bold,
  fontSize: 15,
);

/// RGB max value for random color generation
const int rgbMax = 256;

///Opacity value.
const double opacity = 1.0;
